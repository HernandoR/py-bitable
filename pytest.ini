[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --disable-warnings

[tool:pytest]
markers =
    unit: Unit tests
    integration: Integration tests requiring credentials
